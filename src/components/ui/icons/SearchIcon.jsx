import React from 'react';
import { IoSearchOutline } from 'react-icons/io5';

export default function SearchIcon({ className = '' }) {
  return <IoSearchOutline className={`w-6 h-6 ${className}`} />;
}
